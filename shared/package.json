{
  "name": "@consulta/shared",
  "version": "1.0.0",
  "private": true,
  "description": "Contrato compartilhado entre frontend e backend (schemas Zod, CNJ, tribunais, datas, movimentos).",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "development": "./src/index.ts",
      "default": "./dist/index.js"
    }
  },
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "typescript": "~5.7.3",
    "vitest": "^3.1.1"
  }
}
